global:
  scrape_interval: 5s

scrape_configs:

  - job_name: "system_metrics"
    static_configs:
      - targets: ["localhost:8000"]

  - job_name: "inference_metrics"
    static_configs:
      - targets: ["localhost:8001"]